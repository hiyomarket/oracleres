# 主系統 Agent 整合指令：TASK-004 美術素材匯入

**發送者**：遊戲 Agent
**接收者**：主系統 Agent
**日期**：2026-03-23
**優先級**：🔴 高

---

## 任務背景

先提出我的看法：美術 Agent 已經超前部署，完成了 TASK-004 的 150 張五行色系服裝素材（包含男女各 5 套五行基礎服裝的三視角）。這是一個**高衝擊目標**，能讓用戶一進入靈相空間，衣櫃裡就有足夠的衣服可以完成每日穿搭任務，直接啟動核心循環。

再來執行你的提案：請主系統 Agent 協助將這些素材匯入資料庫，作為 `game_items` 的初始種子資料（Seed Data），並替換掉目前前端 `/game/avatar` 使用的 Placeholder。

---

## 執行步驟

### 1. 讀取 Seed Data JSON
我已經將 `ART/MANIFEST.json` 中屬於 TASK-004 的素材，整理成結構化的 JSON 檔案，存放在：
`MANUS-AGENTS/FOR-GAME/GAME-DESIGN/game-items-seed-task004.json`

### 2. 匯入 `game_items` 資料表
請撰寫一個 Seed Script（例如 `server/db/seed-game-items.ts`），讀取上述 JSON 檔案，並將資料寫入 `game_items` 資料表。

**資料對應規則：**
- `name` -> `name`
- `layer` -> `layer`
- `wuxing` -> `wuxing`
- `rarity` -> `rarity` (皆為 'common')
- `currency_type` -> `currencyType` (皆為 'initial')
- `price` -> `price` (皆為 0)
- `is_initial` -> `is_initial` (皆為 1，代表這是命盤連動初始外觀可選的庫存)
- `file_path` -> `imageUrl` (請注意：由於目前 `cdn_url` 為空，請先直接寫入 `file_path`，或由主系統統一上傳 S3 後替換為 CDN URL)

### 3. 前端渲染適配
目前前端 `AvatarRenderer.tsx` 應該是寫死或使用 Placeholder。請確保前端能正確讀取 `game_wardrobe` 中的 `imageUrl`，並根據用戶選擇的視角（front / left45 / right45）動態切換對應的圖片。

---

## 視覺重點配色提醒

在實作前端商城或衣櫃介面時，請確保五行標籤或篩選按鈕，採用以下**重點配色**：
- **木**：翠綠 `#2E8B57`
- **火**：朱紅 `#DC143C`
- **土**：琥珀棕 `#CD853F`
- **金**：銀白 `#C0C0C0` 或 天命金 `#C9A227`
- **水**：深海藍 `#1E3A5F` 或 量子青 `#00CED1`

完成後請通知 Boss，我們就可以進行靈相換裝系統的完整端到端測試了！
