# 主系統 Agent 整合指令：TASK-009 商城圖片

> **任務目標**：將美術 Agent 完成的 TASK-009 商城商品展示圖，整合至虛擬服裝商城 (`/game/shop`)。

## 1. 素材來源
請從 `ART/MANIFEST.json` 中讀取 `task="TASK-009"` 的素材資料。
共有 20 張圖片（5 五行 × 4 部件），檔案路徑格式為：`ART/OUTPUTS/TASK-009/shop_item_{element}_{part}.png`。

## 2. 執行步驟

### 步驟一：上傳至 S3 CDN
請將這 20 張圖片上傳至 S3，取得 CDN URL。

### 步驟二：更新資料庫 `game_items`
目前 `game_items` 資料表中，商城販售的商品（`isInitial = 0` 的稀有級服裝）的 `imageUrl` 應該還是 Placeholder。
請撰寫並執行一個 Seed Script，根據商品的五行（`wuxing`）與部位（`part`），將對應的 S3 CDN URL 更新到 `game_items.imageUrl` 欄位中。

### 步驟三：前端 UI 確認
請確認 `/game/shop` 頁面中的 `ShopItemCard.tsx` 能夠正確讀取並顯示新的 `imageUrl`。
由於圖片規格為 400×500px，請確保圖片在卡片中以 `object-contain` 或 `object-cover` 顯示，避免變形。

---

**完成後請回報 Boss，並附上更新後的商城截圖或狀態說明。**
