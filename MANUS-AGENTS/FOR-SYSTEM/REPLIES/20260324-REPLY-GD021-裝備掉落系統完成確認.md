# 回覆確認：GD-021 裝備掉落系統 + V26 佈局大修正

**回覆日期：** 2026-03-24  
**對應指令：** 20260324-GD021-裝備掉落系統開發指令.md  
**版本：** V26 (d7d0b276)

---

## ✅ P0 完成項目

### 資料庫新增五張表
- `equipment_templates`：裝備模板（id、名稱、屬性、稀有度、五行、等級需求、加成數值）
- `monster_drop_tables`：怪物掉落表（monsterId、equipmentId、baseDropRate 千分比）
- `agent_drop_counters`：低保計數器（noDropStreak、noMidStreak、noHighStreak、totalBattles）
- `agent_set_progress`：套裝進度（agentId、setId、pieceCount、isComplete）
- `agent_inventory` 新增欄位：`is_equipped`（0/1）、`equipped_slot`（裝備位置）

### 木屬性初階裝備種子資料（6件）
| 裝備 | 屬性 | 稀有度 | HP加成 | ATK加成 |
|------|------|--------|--------|---------|
| 翠葉護腕 | 木 | basic | +15 | — |
| 青藤腰帶 | 木 | basic | +10 | — |
| 木靈短劍 | 木 | basic | — | +8 |
| 翠林護甲 | 木 | mid | +25 | — |
| 木靈法杖 | 木 | mid | — | +12 |
| 翠龍長劍 | 木 | high | — | +20 |

### 怪物掉落表（22筆）
涵蓋 10 隻木屬性怪物（山精、林妖、木靈、藤蔓妖、樹人等），基礎掉落率 30‰～80‰。

---

## ✅ P1 完成項目

### tickEngine 命格共鳴掉落計算
- `calculateFinalDropRate()`：五行剋制加成（×1.5/×0.8/×1.0）+ 命格共鳴加成（±50%）+ 幸運加成（金屬性/1000）+ 任務加成（×1.2）
- `checkPityAndGetGuaranteedTier()`：低保檢查（15連無掉落→basic、30連→mid、75連→high）
- `updateDropCounters()`：每次戰鬥後更新計數器
- `rollEquipmentDrops()`：整合以上邏輯，從資料庫查詢掉落表計算最終掉落

### 新增 tRPC 端點
- `gameWorld.getDroppedInventory`：查詢背包中的掉落裝備（含模板資訊）
- `gameWorld.equipDroppedItem`：從背包裝備掉落裝備到指定槽位
- `gameWorld.getDropCounters`：查詢低保計數器狀態

---

## ✅ 同批次完成的其他修正

### 佈局大修正（★★★★★）
- 手機版把手列加入 HP/MP/AP/體力的小型條狀顯示，玩家不需展開角色面板即可查看狀態
- 靈相干預按鈕移至地圖右上角固定（z-index 450），不受角色面板展開影響
- 行動策略按鈕移至靈相下方固定（z-index 445），展開方向改為向下
- DraggableWidget 加入 touch 事件支援（手機可拖拉）
- 五個浮動區塊設定分散的預設位置，不再疊在角色面板下

### 體力恢復機制
- 改為每 **30 分鐘 +30 點**（原為 20 分鐘 +1 點）
- tickEngine、getAgentStatus 兩處同步更新
- 前端體力條旁顯示「下次恢復：X 分鐘後（+30）」

### 技能安裝 Bug 修正
- `installSkill` mutation 的 slot 枚舉加入 `skillSlot3`、`skillSlot4`、`passiveSlot2`、`hiddenSlot1`
- 前端技能面板已支援動態槽位擴充（木屬性門檻）

---

## 📋 待辦事項（建議下次處理）

1. 後台 Admin 頁面新增「裝備模板管理」Tab，讓企劃可以直接在 UI 中新增/編輯裝備
2. 前端道具 Tab 的「掉落裝備」區塊顯示（目前後端 API 已就緒，前端 UI 待建立）
3. 套裝系統（agentSetProgress）的觸發邏輯和套裝加成效果

---

*由 Manus AI Agent 自動生成*
