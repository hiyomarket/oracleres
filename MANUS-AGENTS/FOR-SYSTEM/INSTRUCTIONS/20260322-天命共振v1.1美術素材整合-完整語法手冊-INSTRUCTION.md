# 天命共振 v1.1 美術素材整合 — 完整語法手冊

> **建立時間**：2026/03/22
> **建立者**：Manus System Agent（美術素材巡邏任務）
> **狀態**：✅ 已執行完成，本文件供後續 Agent 參考複用
> **對應任務**：TASK-001 v1.1 美術素材整合
> **Checkpoint 版本**：`bb400d90`（oracle-resonance 專案）

---

## 📌 本文件用途

本文件記錄本次美術素材巡邏任務的**完整執行語法**，包含：
1. 素材下載與 CDN 上傳流程
2. 所有新增/修改的程式碼（可直接複製貼上）
3. GitHub 同步指令
4. vitest 測試語法

**後續 Agent 可直接讀取本文件，照語法執行後續功能開發。**

---

## 🗂️ 專案資訊

| 項目 | 值 |
|------|-----|
| **Manus 專案名稱** | oracle-resonance |
| **本地路徑** | `/home/ubuntu/oracle-resonance` |
| **GitHub 倉庫** | `hiyomarket/oracleres` |
| **Dev Server** | `https://3000-ipu0z2ag8nugh876wakyv-9517e836.sg1.manus.computer` |
| **已發布網域** | `destinyweb-svfigzte.manus.space` |
| **Checkpoint** | `bb400d90` |
| **技術棧** | React 19 + Tailwind 4 + Express 4 + tRPC 11 + Drizzle ORM |

---

## 🎨 美術素材 CDN URL 對照表

> **所有素材均已上傳至 Manus CDN，可直接使用以下 URL**

| 素材 Key | 用途 | CDN URL |
|----------|------|---------|
| `landing-hero-bg` | Hero 背景圖（2752×1536） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/hChTlYALXKlQYYHK.png` |
| `landing-hero-orb` | 命盤光球（2048×2048，透明背景） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/xTiPmUdIAkfCoSJN.png` |
| `icon-destiny-card` | 命格身份證 icon（2048×2048） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/ZFvnAJUJlzuVEglU.png` |
| `icon-war-room` | 今日作戰室 icon（2048×2048） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/BJHWLyfZZKQMjTtB.png` |
| `icon-oracle` | 天命問卜 icon（2048×2048） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/MpBerwmFyOZCscJA.png` |
| `icon-wealth` | 財運羅盤 icon（2048×2048） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/LPLnlcokDBVxAsxH.png` |
| `icon-calendar` | 天命日曆 icon（2048×2048） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/GpZgmWnVnWsuRAXp.png` |
| `icon-jinang` | 數位錦囊 icon（2048×2048，品牌核心） | `https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/RyvtZMsLTpyDpRnT.png` |

> **在程式碼中統一使用 `CDN` 常數物件引用（見下方 `Home.tsx` CDN 區段）**

---

## 📁 新增/修改的檔案清單

| 檔案路徑 | 操作 | 說明 |
|----------|------|------|
| `client/src/index.css` | **修改** | 日/夜雙模式 CSS 變數、動效、玻璃擬態 |
| `client/index.html` | **修改** | 加入 Noto Serif TC 字型、meta 描述 |
| `client/src/App.tsx` | **修改** | defaultTheme="dark"、switchable |
| `server/routers.ts` | **修改** | 新增 artAssets + fortune tRPC 路由 |
| `client/src/components/StarField.tsx` | **新增** | Canvas 星光粒子背景元件 |
| `client/src/components/FortuneCard.tsx` | **新增** | 運勢卡片（含三階段開啟動畫） |
| `client/src/components/CountUpNumber.tsx` | **新增** | 數字滾動動畫元件 |
| `client/src/pages/Home.tsx` | **修改** | 完整首頁（Hero + 錦囊 + 六大卡片 + 數據牆 + 見證） |
| `server/fortune.test.ts` | **新增** | vitest 測試（8 tests） |

---

## 💻 完整程式碼

### 1. `client/index.html`

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1" />
    <title>天命共振 | 數位錦囊</title>
    <meta name="description" content="天命共振 — 你的數位命理秘書，融合東方玄學與現代科技，解讀命盤、指引每日運勢" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+TC:wght@300;400;500;600;700&family=Noto+Sans+TC:wght@300;400;500;700&display=swap" rel="stylesheet" />
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
    <script defer src="%VITE_ANALYTICS_ENDPOINT%/umami" data-website-id="%VITE_ANALYTICS_WEBSITE_ID%"></script>
  </body>
</html>
```

---

### 2. `client/src/App.tsx`

```tsx
import { Toaster } from "@/components/ui/sonner";
import { TooltipProvider } from "@/components/ui/tooltip";
import NotFound from "@/pages/NotFound";
import { Route, Switch } from "wouter";
import ErrorBoundary from "./components/ErrorBoundary";
import { ThemeProvider } from "./contexts/ThemeContext";
import Home from "./pages/Home";

function Router() {
  return (
    <Switch>
      <Route path={"/"} component={Home} />
      <Route path={"/404"} component={NotFound} />
      <Route component={NotFound} />
    </Switch>
  );
}

function App() {
  return (
    <ErrorBoundary>
      <ThemeProvider defaultTheme="dark" switchable>
        <TooltipProvider>
          <Toaster />
          <Router />
        </TooltipProvider>
      </ThemeProvider>
    </ErrorBoundary>
  );
}

export default App;
```

---

### 3. `client/src/index.css`（完整內容）

```css
@import "tailwindcss";
@import "tw-animate-css";

@custom-variant dark (&:is(.dark *));

@theme inline {
  --radius-sm: calc(var(--radius) - 4px);
  --radius-md: calc(var(--radius) - 2px);
  --radius-lg: var(--radius);
  --radius-xl: calc(var(--radius) + 4px);
  --color-background: var(--background);
  --color-foreground: var(--foreground);
  --color-card: var(--card);
  --color-card-foreground: var(--card-foreground);
  --color-popover: var(--popover);
  --color-popover-foreground: var(--popover-foreground);
  --color-primary: var(--primary);
  --color-primary-foreground: var(--primary-foreground);
  --color-secondary: var(--secondary);
  --color-secondary-foreground: var(--secondary-foreground);
  --color-muted: var(--muted);
  --color-muted-foreground: var(--muted-foreground);
  --color-accent: var(--accent);
  --color-accent-foreground: var(--accent-foreground);
  --color-destructive: var(--destructive);
  --color-destructive-foreground: var(--destructive-foreground);
  --color-border: var(--border);
  --color-input: var(--input);
  --color-ring: var(--ring);
  --color-gold: var(--gold);
  --color-cyan-accent: var(--cyan-accent);
}

/* ── 日間模式：#E8D8F0 淡玫瑰粉 ── */
:root {
  --radius: 0.75rem;
  --background: oklch(91% 0.04 320);
  --foreground: oklch(18% 0.02 260);
  --card: oklch(96% 0.025 320 / 0.75);
  --card-foreground: oklch(18% 0.02 260);
  --popover: oklch(98% 0.015 320);
  --popover-foreground: oklch(18% 0.02 260);
  --primary: oklch(68% 0.13 75);
  --primary-foreground: oklch(10% 0.02 260);
  --secondary: oklch(85% 0.05 320);
  --secondary-foreground: oklch(25% 0.03 260);
  --muted: oklch(88% 0.035 320);
  --muted-foreground: oklch(45% 0.04 280);
  --accent: oklch(68% 0.14 195);
  --accent-foreground: oklch(10% 0.02 260);
  --destructive: oklch(55% 0.22 25);
  --destructive-foreground: oklch(98% 0 0);
  --border: oklch(78% 0.05 320 / 0.5);
  --input: oklch(86% 0.04 320);
  --ring: oklch(68% 0.13 75);
  --gold: oklch(68% 0.13 75);
  --cyan-accent: oklch(68% 0.14 195);
}

/* ── 夜間模式：#1A1A2A 深灰藍 ── */
.dark {
  --background: oklch(14% 0.02 260);
  --foreground: oklch(92% 0.03 320);
  --card: oklch(18% 0.025 260 / 0.75);
  --card-foreground: oklch(92% 0.03 320);
  --popover: oklch(16% 0.02 260);
  --popover-foreground: oklch(92% 0.03 320);
  --primary: oklch(68% 0.13 75);
  --primary-foreground: oklch(10% 0.02 260);
  --secondary: oklch(22% 0.03 260);
  --secondary-foreground: oklch(85% 0.03 320);
  --muted: oklch(22% 0.03 260);
  --muted-foreground: oklch(62% 0.04 300);
  --accent: oklch(68% 0.14 195);
  --accent-foreground: oklch(10% 0.02 260);
  --destructive: oklch(55% 0.22 25);
  --destructive-foreground: oklch(98% 0 0);
  --border: oklch(30% 0.04 260 / 0.5);
  --input: oklch(22% 0.03 260);
  --ring: oklch(68% 0.13 75);
  --gold: oklch(68% 0.13 75);
  --cyan-accent: oklch(68% 0.14 195);
}

@layer base {
  * { @apply border-border outline-ring/50; }
  body {
    @apply bg-background text-foreground;
    font-family: 'Noto Serif TC', 'Noto Sans TC', serif;
    overflow-x: hidden;
  }
  button:not(:disabled), [role="button"]:not([aria-disabled="true"]),
  [type="button"]:not(:disabled), [type="submit"]:not(:disabled),
  [type="reset"]:not(:disabled), a[href], select:not(:disabled),
  input[type="checkbox"]:not(:disabled), input[type="radio"]:not(:disabled) {
    @apply cursor-pointer;
  }
}

@layer components {
  .container {
    width: 100%; margin-left: auto; margin-right: auto;
    padding-left: 1rem; padding-right: 1rem;
  }
  .flex { min-height: 0; min-width: 0; }
  @media (min-width: 640px) { .container { padding-left: 1.5rem; padding-right: 1.5rem; } }
  @media (min-width: 1024px) { .container { padding-left: 2rem; padding-right: 2rem; max-width: 1280px; } }

  /* 玻璃擬態卡片 */
  .glass-card {
    background: var(--card);
    backdrop-filter: blur(16px) saturate(180%);
    -webkit-backdrop-filter: blur(16px) saturate(180%);
    border: 1px solid oklch(68% 0.13 75 / 0.25);
    border-radius: var(--radius-lg);
    transition: all 0.35s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .glass-card:hover {
    border-color: oklch(68% 0.13 75 / 0.65);
    box-shadow: 0 0 24px oklch(68% 0.13 75 / 0.2), 0 8px 32px oklch(0% 0 0 / 0.25), inset 0 1px 0 oklch(100% 0 0 / 0.1);
    transform: translateY(-4px);
  }

  /* 天命金文字漸層 */
  .text-gold-gradient {
    background: linear-gradient(135deg, #C9A227 0%, #F5D06A 50%, #C9A227 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* 全息漸層文字（動態流動） */
  .text-holographic {
    background: linear-gradient(135deg, #C9A227 0%, #E8D8F0 30%, #00CED1 60%, #C9A227 100%);
    background-size: 200% 200%;
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    animation: holographic-shift 4s ease infinite;
  }

  /* 天命金脈衝光效 */
  .gold-pulse { animation: gold-pulse-anim 2s ease-in-out infinite; }

  /* 命盤光球浮動 */
  .orb-float { animation: orb-float 6s ease-in-out infinite; }

  /* 錦囊開啟動畫 */
  .jinang-opening { animation: jinang-open 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) forwards; }

  /* 滾動條美化 */
  ::-webkit-scrollbar { width: 6px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: oklch(68% 0.13 75 / 0.4); border-radius: 3px; }
  ::-webkit-scrollbar-thumb:hover { background: oklch(68% 0.13 75 / 0.8); }
}

/* ── 動效關鍵影格 ── */
@keyframes holographic-shift {
  0%, 100% { background-position: 0% 50%; }
  50% { background-position: 100% 50%; }
}
@keyframes gold-pulse-anim {
  0%, 100% { box-shadow: 0 0 8px oklch(68% 0.13 75 / 0.4), 0 0 16px oklch(68% 0.13 75 / 0.2); }
  50% { box-shadow: 0 0 20px oklch(68% 0.13 75 / 0.8), 0 0 40px oklch(68% 0.13 75 / 0.4), 0 0 60px oklch(68% 0.13 75 / 0.2); }
}
@keyframes orb-float {
  0%, 100% { transform: translateY(0px) rotate(0deg); }
  33% { transform: translateY(-14px) rotate(2deg); }
  66% { transform: translateY(-7px) rotate(-1deg); }
}
@keyframes jinang-open {
  0% { transform: scale(0.7) rotate(-8deg); opacity: 0; }
  40% { transform: scale(1.12) rotate(3deg); opacity: 1; }
  70% { transform: scale(0.96) rotate(-1deg); }
  100% { transform: scale(1) rotate(0deg); opacity: 1; }
}
@keyframes twinkle {
  0%, 100% { opacity: 0.15; transform: scale(0.7); }
  50% { opacity: 1; transform: scale(1.3); }
}
@keyframes ripple-expand {
  0% { transform: scale(0.5); opacity: 0.8; }
  100% { transform: scale(2); opacity: 0; }
}
@keyframes slide-up {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

.animate-slide-up { animation: slide-up 0.7s ease-out forwards; }
.animate-fade-in { animation: fade-in 0.5s ease-out forwards; }
.animate-count-up { animation: count-up 0.6s ease-out forwards; }
```

---

### 4. `server/routers.ts`（完整內容）

```typescript
import { z } from "zod";
import { COOKIE_NAME } from "@shared/const";
import { getSessionCookieOptions } from "./_core/cookies";
import { systemRouter } from "./_core/systemRouter";
import { publicProcedure, router } from "./_core/trpc";
import { invokeLLM } from "./_core/llm";

// ── 美術素材 CDN URL 對照表 ──────────────────────────────
const ART_CDN_URLS = {
  "landing-hero-bg":    "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/hChTlYALXKlQYYHK.png",
  "landing-hero-orb":   "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/xTiPmUdIAkfCoSJN.png",
  "icon-destiny-card":  "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/ZFvnAJUJlzuVEglU.png",
  "icon-war-room":      "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/BJHWLyfZZKQMjTtB.png",
  "icon-oracle":        "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/MpBerwmFyOZCscJA.png",
  "icon-wealth":        "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/LPLnlcokDBVxAsxH.png",
  "icon-calendar":      "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/GpZgmWnVnWsuRAXp.png",
  "icon-jinang":        "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/RyvtZMsLTpyDpRnT.png",
} as const;

export const appRouter = router({
  system: systemRouter,

  auth: router({
    me: publicProcedure.query(opts => opts.ctx.user),
    logout: publicProcedure.mutation(({ ctx }) => {
      const cookieOptions = getSessionCookieOptions(ctx.req);
      ctx.res.clearCookie(COOKIE_NAME, { ...cookieOptions, maxAge: -1 });
      return { success: true } as const;
    }),
  }),

  // ── 美術素材巡邏 API ─────────────────────────────────
  artAssets: router({
    getCdnUrls: publicProcedure.query(() => ART_CDN_URLS),
  }),

  // ── 今日運勢 API ─────────────────────────────────────
  fortune: router({
    getToday: publicProcedure
      .input(z.object({
        birthdate: z.string().regex(
          /^\d{4}-\d{2}-\d{2}$/,
          "請輸入正確的生日格式 YYYY-MM-DD"
        ),
      }))
      .mutation(async ({ input }) => {
        const today = new Date().toLocaleDateString("zh-TW", {
          year: "numeric", month: "long", day: "numeric",
          weekday: "long", timeZone: "Asia/Taipei",
        });

        const prompt = `你是天命共振的命理 AI，融合東方玄學與現代科技。
用戶生日：${input.birthdate}
今日日期：${today}
請根據用戶生日，提供今日運勢分析。回應必須是 JSON 格式，包含以下欄位：
{
  "overall": "整體運勢（1-5顆星，用 ★ 表示）",
  "overall_score": 整體分數（1-5的整數）,
  "summary": "今日運勢總結（50字以內，充滿神秘感與東方美學）",
  "love": "感情運（30字以內）",
  "career": "事業運（30字以內）",
  "wealth": "財運（30字以內）",
  "health": "健康運（30字以內）",
  "lucky_color": "今日幸運色",
  "lucky_number": 幸運數字（1-99的整數）,
  "advice": "天命錦囊建議（60字以內，古風詩意風格）",
  "element": "今日主導五行（金/木/水/火/土）"
}`;

        try {
          const response = await invokeLLM({
            messages: [
              { role: "system", content: "你是天命共振命理 AI，專精東方命理與現代科技融合。請嚴格回傳 JSON 格式。" },
              { role: "user", content: prompt },
            ],
            response_format: {
              type: "json_schema",
              json_schema: {
                name: "fortune_reading",
                strict: true,
                schema: {
                  type: "object",
                  properties: {
                    overall: { type: "string" },
                    overall_score: { type: "number" },
                    summary: { type: "string" },
                    love: { type: "string" },
                    career: { type: "string" },
                    wealth: { type: "string" },
                    health: { type: "string" },
                    lucky_color: { type: "string" },
                    lucky_number: { type: "number" },
                    advice: { type: "string" },
                    element: { type: "string" },
                  },
                  required: ["overall","overall_score","summary","love","career","wealth","health","lucky_color","lucky_number","advice","element"],
                  additionalProperties: false,
                },
              },
            },
          });

          const rawContent = response.choices[0]?.message?.content;
          const content = typeof rawContent === 'string' ? rawContent : null;
          if (!content) throw new Error("No response from LLM");
          const fortune = JSON.parse(content);
          return { success: true, fortune, birthdate: input.birthdate };

        } catch (_error) {
          // 備用靜態運勢（LLM 不可用時）
          return {
            success: true,
            fortune: {
              overall: "★★★★",
              overall_score: 4,
              summary: "天地交泰，萬象更新。今日氣場流動，貴人自遠方來，把握機緣，事半功倍。",
              love: "桃花運旺，緣分悄然降臨，保持開放心態。",
              career: "思維敏銳，創意湧現，適合推進重要計畫。",
              wealth: "財星高照，意外之財有望，謹慎投資為宜。",
              health: "精力充沛，注意作息規律，避免過度勞累。",
              lucky_color: "天命金",
              lucky_number: 8,
              advice: "靜水流深，厚積薄發。今日宜謀不宜動，靜觀其變，待時而動。",
              element: "金",
            },
            birthdate: input.birthdate,
          };
        }
      }),
  }),
});

export type AppRouter = typeof appRouter;
```

---

### 5. `client/src/components/StarField.tsx`（新增）

```tsx
import { useEffect, useRef } from "react";

interface Star {
  x: number; y: number; size: number;
  opacity: number; speed: number; color: string;
}

export default function StarField({ className = "" }: { className?: string }) {
  const canvasRef = useRef<HTMLCanvasElement>(null);
  const starsRef = useRef<Star[]>([]);
  const animRef = useRef<number>(0);

  useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    if (!ctx) return;

    const colors = ["#C9A227", "#E8D8F0", "#00CED1", "#ffffff", "#F5D06A"];

    const resize = () => {
      canvas.width = canvas.offsetWidth;
      canvas.height = canvas.offsetHeight;
      starsRef.current = Array.from({ length: 120 }, () => ({
        x: Math.random() * canvas.width,
        y: Math.random() * canvas.height,
        size: Math.random() * 2.5 + 0.5,
        opacity: Math.random() * 0.7 + 0.1,
        speed: Math.random() * 0.3 + 0.05,
        color: colors[Math.floor(Math.random() * colors.length)],
      }));
    };

    resize();
    window.addEventListener("resize", resize);

    let time = 0;
    const animate = () => {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      time += 0.01;
      starsRef.current.forEach((star, i) => {
        const twinkle = Math.sin(time * star.speed * 5 + i) * 0.4 + 0.6;
        ctx.beginPath();
        ctx.arc(star.x, star.y, star.size * twinkle, 0, Math.PI * 2);
        ctx.fillStyle = star.color;
        ctx.globalAlpha = star.opacity * twinkle;
        ctx.fill();
        // 金色星星加光暈
        if (star.color === "#C9A227" && star.size > 1.5) {
          const gradient = ctx.createRadialGradient(star.x, star.y, 0, star.x, star.y, star.size * 4 * twinkle);
          gradient.addColorStop(0, "rgba(201,162,39,0.3)");
          gradient.addColorStop(1, "rgba(201,162,39,0)");
          ctx.beginPath();
          ctx.arc(star.x, star.y, star.size * 4 * twinkle, 0, Math.PI * 2);
          ctx.fillStyle = gradient;
          ctx.globalAlpha = 0.5;
          ctx.fill();
        }
      });
      ctx.globalAlpha = 1;
      animRef.current = requestAnimationFrame(animate);
    };
    animate();

    return () => {
      window.removeEventListener("resize", resize);
      cancelAnimationFrame(animRef.current);
    };
  }, []);

  return (
    <canvas
      ref={canvasRef}
      className={`absolute inset-0 w-full h-full pointer-events-none ${className}`}
      aria-hidden="true"
    />
  );
}
```

---

### 6. `client/src/components/CountUpNumber.tsx`（新增）

```tsx
import { useEffect, useRef, useState } from "react";

interface CountUpNumberProps {
  target: number;
  suffix?: string;
  prefix?: string;
  duration?: number;
  className?: string;
}

export default function CountUpNumber({
  target, suffix = "", prefix = "", duration = 2000, className = "",
}: CountUpNumberProps) {
  const [count, setCount] = useState(0);
  const [started, setStarted] = useState(false);
  const ref = useRef<HTMLSpanElement>(null);

  useEffect(() => {
    const observer = new IntersectionObserver(
      ([entry]) => { if (entry.isIntersecting && !started) setStarted(true); },
      { threshold: 0.3 }
    );
    if (ref.current) observer.observe(ref.current);
    return () => observer.disconnect();
  }, [started]);

  useEffect(() => {
    if (!started) return;
    const startTime = performance.now();
    const animate = (currentTime: number) => {
      const elapsed = currentTime - startTime;
      const progress = Math.min(elapsed / duration, 1);
      const eased = 1 - Math.pow(1 - progress, 3); // easeOutCubic
      setCount(Math.floor(eased * target));
      if (progress < 1) requestAnimationFrame(animate);
    };
    requestAnimationFrame(animate);
  }, [started, target, duration]);

  return (
    <span ref={ref} className={className}>
      {prefix}{count.toLocaleString()}{suffix}
    </span>
  );
}
```

---

### 7. `client/src/components/FortuneCard.tsx`（新增）

```tsx
import { useState, useEffect } from "react";

interface Fortune {
  overall: string; overall_score: number; summary: string;
  love: string; career: string; wealth: string; health: string;
  lucky_color: string; lucky_number: number; advice: string; element: string;
}
interface FortuneCardProps {
  fortune: Fortune;
  birthdate: string;
  onClose?: () => void;
}

const ELEMENT_COLORS: Record<string, string> = {
  金: "#C9A227", 木: "#4CAF50", 水: "#00CED1", 火: "#FF6B6B", 土: "#D4A574",
};
const ASPECT_ICONS: Record<string, string> = {
  love: "♥", career: "⚡", wealth: "◈", health: "✦",
};

const JINANG_CDN = "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/RyvtZMsLTpyDpRnT.png";

export default function FortuneCard({ fortune, birthdate, onClose }: FortuneCardProps) {
  const [visible, setVisible] = useState(false);
  const [phase, setPhase] = useState<"bag" | "opening" | "content">("bag");

  useEffect(() => {
    setVisible(true);
    const t1 = setTimeout(() => setPhase("opening"), 300);
    const t2 = setTimeout(() => setPhase("content"), 1100);
    return () => { clearTimeout(t1); clearTimeout(t2); };
  }, []);

  const elementColor = ELEMENT_COLORS[fortune.element] || "#C9A227";
  const aspects = [
    { key: "love", label: "感情運", value: fortune.love },
    { key: "career", label: "事業運", value: fortune.career },
    { key: "wealth", label: "財運", value: fortune.wealth },
    { key: "health", label: "健康運", value: fortune.health },
  ];

  return (
    <div className={`transition-all duration-500 ${visible ? "opacity-100 translate-y-0" : "opacity-0 translate-y-8"}`}>
      {/* 錦囊開啟動畫（bag → opening → content） */}
      {phase !== "content" && (
        <div className="flex flex-col items-center justify-center py-12 gap-4">
          <div className={`relative ${phase === "opening" ? "jinang-opening" : ""}`} style={{ width: 120, height: 120 }}>
            <img src={JINANG_CDN} alt="數位錦囊" className="w-full h-full object-contain drop-shadow-[0_0_20px_rgba(201,162,39,0.8)]" />
            {phase === "opening" && (
              <div className="absolute inset-0 flex items-center justify-center">
                <div className="w-full h-full rounded-full border-2 border-[#C9A227]/60 animate-ping" />
              </div>
            )}
          </div>
          <p className="text-[#C9A227] font-medium animate-pulse text-sm tracking-widest">
            {phase === "bag" ? "開啟天命錦囊..." : "解讀命盤中..."}
          </p>
        </div>
      )}

      {/* 運勢內容 */}
      {phase === "content" && (
        <div className="space-y-4 animate-slide-up">
          {/* 標題列 */}
          <div className="flex items-center justify-between">
            <div>
              <p className="text-xs text-muted-foreground tracking-widest mb-1">{birthdate} 的今日天命</p>
              <div className="flex items-center gap-2">
                <span className="text-2xl text-[#C9A227]">{fortune.overall}</span>
                <span className="text-xs px-2 py-0.5 rounded-full font-medium"
                  style={{ backgroundColor: elementColor + "20", color: elementColor, border: `1px solid ${elementColor}40` }}>
                  {fortune.element}行主導
                </span>
              </div>
            </div>
            {onClose && (
              <button onClick={onClose} className="text-muted-foreground hover:text-foreground transition-colors text-lg leading-none" aria-label="關閉">✕</button>
            )}
          </div>

          {/* 運勢總結 */}
          <div className="rounded-xl p-4 text-center"
            style={{ background: `linear-gradient(135deg, ${elementColor}15, transparent)`, border: `1px solid ${elementColor}30` }}>
            <p className="text-sm leading-relaxed text-foreground/90 font-medium">{fortune.summary}</p>
          </div>

          {/* 四大運勢 */}
          <div className="grid grid-cols-2 gap-2">
            {aspects.map(({ key, label, value }) => (
              <div key={key} className="glass-card rounded-xl p-3">
                <div className="flex items-center gap-1.5 mb-1">
                  <span className="text-[#C9A227] text-xs">{ASPECT_ICONS[key]}</span>
                  <span className="text-xs font-medium text-muted-foreground">{label}</span>
                </div>
                <p className="text-xs leading-relaxed text-foreground/80">{value}</p>
              </div>
            ))}
          </div>

          {/* 幸運資訊 */}
          <div className="flex gap-3">
            <div className="flex-1 glass-card rounded-xl p-3 text-center">
              <p className="text-xs text-muted-foreground mb-1">幸運色</p>
              <p className="text-sm font-medium text-[#C9A227]">{fortune.lucky_color}</p>
            </div>
            <div className="flex-1 glass-card rounded-xl p-3 text-center">
              <p className="text-xs text-muted-foreground mb-1">幸運數字</p>
              <p className="text-xl font-bold text-[#C9A227]">{fortune.lucky_number}</p>
            </div>
          </div>

          {/* 天命錦囊建議 */}
          <div className="rounded-xl p-4"
            style={{ background: "linear-gradient(135deg, rgba(201,162,39,0.1), rgba(0,206,209,0.05))", border: "1px solid rgba(201,162,39,0.3)" }}>
            <div className="flex items-center gap-2 mb-2">
              <img src={JINANG_CDN} alt="錦囊" className="w-5 h-5 object-contain" />
              <span className="text-xs font-medium text-[#C9A227] tracking-widest">天命錦囊</span>
            </div>
            <p className="text-sm leading-relaxed text-foreground/90 italic">{fortune.advice}</p>
          </div>
        </div>
      )}
    </div>
  );
}
```

---

### 8. `client/src/pages/Home.tsx`（完整內容）

> **注意**：此檔案較長，完整版請直接參考 oracle-resonance 專案 checkpoint `bb400d90`。
> 以下為關鍵結構摘要，可直接在 Manus 平台查看完整程式碼。

**檔案結構：**
```
imports（useState, useEffect, useRef, useAuth, getLoginUrl, trpc, useTheme, StarField, FortuneCard, CountUpNumber, toast）
  ↓
CDN 常數物件（8 個素材 URL）
  ↓
FEATURE_CARDS 陣列（6 個功能卡片資料）
  ↓
TESTIMONIALS 陣列（3 則用戶見證）
  ↓
STATS 陣列（4 個數據：5000+, 98%, 365, 12）
  ↓
Home() 元件
  ├── useAuth() → { user, isAuthenticated, logout }
  ├── useTheme() → { theme, toggleTheme }
  ├── useState: birthdate, fortune, showFortune, testimonialIndex, heroLoaded
  ├── trpc.fortune.getToday.useMutation()
  ├── useEffect: 見證輪播（4 秒自動切換）
  ├── handleFortune(): 驗證生日格式 → 呼叫 mutation
  └── JSX 結構：
      ├── <nav> 固定導覽列（Logo + 日夜切換 + 登入/登出）
      ├── <section> Hero 區（背景圖 + StarField + 文字 + 錦囊表單 + 命盤光球）
      ├── <section> 六大功能卡片（3 欄 grid + 玻璃擬態 + 鎖定遮罩）
      ├── <section> 數據牆（CountUpNumber × 4）
      ├── <section> 用戶見證（桌面 3 欄 / 手機輪播）
      ├── <section> CTA（未登入時顯示「登入後，你的元神將正式覺醒」）
      └── <footer> 頁腳
  ↓
TestimonialCard 子元件
```

**CDN 常數物件（複製此段到 Home.tsx 頂部）：**
```tsx
const CDN = {
  heroBg:          "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/hChTlYALXKlQYYHK.png",
  heroOrb:         "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/xTiPmUdIAkfCoSJN.png",
  iconDestinyCard: "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/ZFvnAJUJlzuVEglU.png",
  iconWarRoom:     "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/BJHWLyfZZKQMjTtB.png",
  iconOracle:      "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/MpBerwmFyOZCscJA.png",
  iconWealth:      "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/LPLnlcokDBVxAsxH.png",
  iconCalendar:    "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/GpZgmWnVnWsuRAXp.png",
  iconJinang:      "https://files.manuscdn.com/user_upload_by_module/session_file/310519663104688923/RyvtZMsLTpyDpRnT.png",
};
```

---

### 9. `server/fortune.test.ts`（vitest 測試）

```typescript
import { describe, expect, it } from "vitest";
import { appRouter } from "./routers";
import type { TrpcContext } from "./_core/context";

function createPublicContext(): TrpcContext {
  return {
    user: null,
    req: { protocol: "https", headers: {} } as TrpcContext["req"],
    res: { clearCookie: () => {} } as TrpcContext["res"],
  };
}

describe("artAssets.getCdnUrls", () => {
  it("returns all 8 CDN URL keys", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    const result = await caller.artAssets.getCdnUrls();
    const expectedKeys = ["landing-hero-bg","landing-hero-orb","icon-destiny-card","icon-war-room","icon-oracle","icon-wealth","icon-calendar","icon-jinang"];
    for (const key of expectedKeys) {
      expect(result).toHaveProperty(key);
      expect(result[key as keyof typeof result]).toMatch(/^https:\/\/files\.manuscdn\.com\//);
    }
  });
  it("has exactly 8 CDN entries", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    const result = await caller.artAssets.getCdnUrls();
    expect(Object.keys(result)).toHaveLength(8);
  });
});

describe("fortune.getToday input validation", () => {
  it("rejects invalid birthdate format", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    await expect(caller.fortune.getToday({ birthdate: "not-a-date" })).rejects.toThrow();
  });
  it("rejects empty birthdate", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    await expect(caller.fortune.getToday({ birthdate: "" })).rejects.toThrow();
  });
  it("rejects partial date", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    await expect(caller.fortune.getToday({ birthdate: "1990-01" })).rejects.toThrow();
  });
  it("accepts valid YYYY-MM-DD format", async () => {
    const ctx = createPublicContext();
    const caller = appRouter.createCaller(ctx);
    const result = await caller.fortune.getToday({ birthdate: "1990-01-01" });
    expect(result).toHaveProperty("success", true);
    expect(result).toHaveProperty("fortune");
    expect(result).toHaveProperty("birthdate", "1990-01-01");
  }, 30000);
});
```

---

## 🔧 後續 Agent 執行指令

### 環境確認

```bash
# 確認專案存在
ls /home/ubuntu/oracle-resonance

# 若不存在，從 checkpoint 恢復（在 Manus 管理介面操作）
# Checkpoint ID: bb400d90

# 確認 dev server 運行
ss -tlnp | grep 3000
```

### 新增功能開發流程

```bash
# 1. 進入專案目錄
cd /home/ubuntu/oracle-resonance

# 2. 確認目前狀態
pnpm test

# 3. 開發新功能後執行測試
pnpm test

# 4. 儲存 checkpoint（在 Manus 工具中執行 webdev_save_checkpoint）

# 5. 更新 STATUS.md
cd /home/ubuntu/oracleres
git add STATUS/
git commit -m "docs(status): update TASK-XXX status"
git push origin main
```

### tRPC 新增路由範例

```typescript
// 在 server/routers.ts 的 appRouter 中新增：
destinyCard: router({
  generate: protectedProcedure
    .input(z.object({ birthdate: z.string().regex(/^\d{4}-\d{2}-\d{2}$/) }))
    .mutation(async ({ input, ctx }) => {
      // 使用 invokeLLM 生成命格身份證
      const response = await invokeLLM({ messages: [...] });
      return { success: true, card: JSON.parse(response.choices[0].message.content) };
    }),
}),
```

### 新增頁面路由範例

```tsx
// 在 client/src/App.tsx 的 Router() 中新增：
import DestinyCard from "./pages/DestinyCard";
// ...
<Route path={"/destiny-card"} component={DestinyCard} />
```

---

## 📊 設計規範速查

### 配色系統

| 用途 | 色值 | CSS 類別 |
|------|------|---------|
| 天命金（主色） | `#C9A227` | `text-[#C9A227]` / `.text-gold-gradient` |
| 天命金淡色 | `#F5D06A` | 用於漸層高光 |
| 電青（輔色） | `#00CED1` | `text-[#00CED1]` |
| 日間背景 | `#E8D8F0` | `bg-background`（light mode） |
| 夜間背景 | `#1A1A2A` | `bg-background`（dark mode） |
| 全息文字 | 動態漸層 | `.text-holographic` |

### 動效類別

| 類別 | 效果 |
|------|------|
| `.glass-card` | 玻璃擬態卡片（含 hover 光暈 + 上移） |
| `.text-gold-gradient` | 天命金文字漸層 |
| `.text-holographic` | 全息流動文字漸層 |
| `.gold-pulse` | 金色脈衝光效（按鈕用） |
| `.orb-float` | 命盤光球浮動動畫 |
| `.jinang-opening` | 錦囊開啟彈跳動畫 |
| `.animate-slide-up` | 向上滑入（0.7s） |
| `.animate-fade-in` | 淡入（0.5s） |

### 響應式斷點

| 斷點 | 寬度 | 用途 |
|------|------|------|
| `sm:` | 640px | 手機橫向 |
| `md:` | 768px | 平板 |
| `lg:` | 1024px | 桌面 |

---

## ✅ 測試結果

```
Test Files  2 passed (2)
     Tests  8 passed (8)
  Duration  5.71s

✓ artAssets.getCdnUrls → returns all 8 CDN URL keys
✓ artAssets.getCdnUrls → has exactly 8 CDN entries
✓ fortune.getToday → rejects invalid birthdate format
✓ fortune.getToday → rejects empty birthdate
✓ fortune.getToday → rejects partial date
✓ fortune.getToday → accepts valid YYYY-MM-DD format
✓ auth.logout → returns success and clears cookie
✓ auth.logout → clears the session cookie and reports success
```

---

## 🔗 相關連結

| 資源 | 連結 |
|------|------|
| **GitHub 倉庫** | `https://github.com/hiyomarket/oracleres` |
| **ART/MANIFEST.json** | `https://github.com/hiyomarket/oracleres/blob/main/ART/MANIFEST.json` |
| **STATUS.md** | `https://github.com/hiyomarket/oracleres/blob/main/STATUS/TASK-001-首頁MVP/STATUS.md` |
| **本指令文** | `https://github.com/hiyomarket/oracleres/blob/main/MANUS-AGENTS/FOR-SYSTEM/INSTRUCTIONS/20260322-天命共振v1.1美術素材整合-完整語法手冊-INSTRUCTION.md` |
| **已發布網站** | `https://destinyweb-svfigzte.manus.space` |

---

*本文件由 Manus System Agent 於 2026/03/22 自動生成，記錄 TASK-001 v1.1 美術素材巡邏任務的完整執行語法。*
