# 開發指令：裝備掉落系統實作（GD-021）

**發出方**：虛相世界沙盒 Agent  
**日期**：2026-03-24  
**優先級**：P0（資料庫設計）+ P1（掉落邏輯）  
**依據文件**：  
- `MANUS-AGENTS/FOR-GAME/GAME-DESIGN/GD-015B-裝備掉落系統完整設計書.md`（主設計文件）  
- `MANUS-AGENTS/FOR-GAME/GAME-DESIGN/GD-021-裝備掉落系統補充與開發規格.md`（本指令的補充規格）

---

## 執行前必讀

**請先讀 GD-021，再讀 GD-015B。** GD-021 包含對 GD-015B 的兩個 P0 修正（ID 命名衝突、掉落率數值不一致），如果先讀 GD-015B 再讀 GD-021，可以確保你用的是最終版本的數值。

---

## 任務一：P0 — 資料庫 Schema 建立

請在 `drizzle/schema.ts` 新增以下五張表，然後執行 `pnpm db:push`：

### 1. equipment_templates（裝備主表）

```typescript
export const equipmentTemplates = mysqlTable('equipment_templates', {
  id: varchar('id', { length: 20 }).primaryKey(),  // E_W001, E_F001 等
  name: varchar('name', { length: 50 }).notNull(),
  element: mysqlEnum('element', ['wood', 'fire', 'earth', 'metal', 'water']).notNull(),
  tier: mysqlEnum('tier', ['basic', 'mid', 'high', 'legendary']).notNull(),
  slot: mysqlEnum('slot', ['weapon', 'helmet', 'armor', 'boots', 'accessory']).notNull(),
  levelReq: int('level_req').default(1).notNull(),
  
  // 基礎屬性加成
  hpBonus: int('hp_bonus').default(0),
  atkBonus: int('atk_bonus').default(0),
  defBonus: int('def_bonus').default(0),
  spdBonus: int('spd_bonus').default(0),
  matkBonus: int('matk_bonus').default(0),
  mpBonus: int('mp_bonus').default(0),
  
  // 特殊詞條（JSON，最多 3 個）
  affixes: json('affixes'),
  
  // 套裝資訊
  setId: varchar('set_id', { length: 20 }),
  setPiece: int('set_piece'),
  
  // 商店定價（NULL = 不可購買）
  shopPrice: int('shop_price'),
  npcSellPrice: int('npc_sell_price'),
  
  // 是否可交易（天命令具設為 false）
  tradeable: boolean('tradeable').default(true),
  
  createdAt: bigint('created_at', { mode: 'number' }).notNull(),
});
```

### 2. monster_drop_tables（怪物掉落表）

```typescript
export const monsterDropTables = mysqlTable('monster_drop_tables', {
  id: int('id').autoincrement().primaryKey(),
  monsterId: varchar('monster_id', { length: 20 }).notNull(),   // M_W001 等
  equipmentId: varchar('equipment_id', { length: 20 }).notNull(), // E_W001 等
  dropRate: decimal('drop_rate', { precision: 5, scale: 4 }).notNull(), // 0.0700 = 7%
  isPenetrate: boolean('is_penetrate').default(false), // 是否為跨階滲透掉落
});
```

### 3. agent_inventory（玩家背包表）

```typescript
export const agentInventory = mysqlTable('agent_inventory', {
  id: int('id').autoincrement().primaryKey(),
  agentId: int('agent_id').notNull(),
  equipmentId: varchar('equipment_id', { length: 20 }).notNull(),
  
  // 個體詞條（每件裝備的隨機詞條）
  rolledAffixes: json('rolled_affixes'),
  
  // 強化等級（0~10）
  enhanceLevel: int('enhance_level').default(0),
  
  // 裝備狀態
  isEquipped: boolean('is_equipped').default(false),
  equippedSlot: mysqlEnum('equipped_slot', ['weapon', 'helmet', 'armor', 'boots', 'accessory']),
  
  // 來源記錄
  obtainedFrom: varchar('obtained_from', { length: 50 }), // 'drop:M_W001', 'shop', 'craft'
  obtainedAt: bigint('obtained_at', { mode: 'number' }).notNull(),
});
```

### 4. agent_drop_counters（低保計數表）

```typescript
export const agentDropCounters = mysqlTable('agent_drop_counters', {
  agentId: int('agent_id').primaryKey(),
  noDropStreak: int('no_drop_streak').default(0),   // 連續未掉落任何裝備的場次
  noMidStreak: int('no_mid_streak').default(0),     // 連續未掉落中階以上的場次
  noHighStreak: int('no_high_streak').default(0),   // 連續未掉落高階以上的場次
  lastUpdated: bigint('last_updated', { mode: 'number' }).notNull(),
});
```

### 5. agent_set_progress（套裝收集進度表）

```typescript
export const agentSetProgress = mysqlTable('agent_set_progress', {
  agentId: int('agent_id').notNull(),
  setId: varchar('set_id', { length: 20 }).notNull(),
  piecesOwned: int('pieces_owned').default(0),
}, (table) => ({
  pk: primaryKey({ columns: [table.agentId, table.setId] }),
}));
```

---

## 任務二：P0 — 種子資料（Seed Data）

建立 `server/seeds/equipment.ts`，將 GD-015B 第五章的裝備與怪物掉落對照表轉為資料庫種子資料。

**優先實作木屬性（最完整）**，其他四個屬性的詳細資料等 Claw Team 補充後再插入。

木屬性初階裝備（E_W001~E_W006）範例：

```typescript
const WOOD_EQUIPMENT_BASIC = [
  { id: 'E_W001', name: '木劍', element: 'wood', tier: 'basic', slot: 'weapon', levelReq: 1, atkBonus: 8, shopPrice: 100, npcSellPrice: 20 },
  { id: 'E_W002', name: '藤蔓弓', element: 'wood', tier: 'basic', slot: 'weapon', levelReq: 1, atkBonus: 7, spdBonus: 2, shopPrice: 120, npcSellPrice: 24 },
  { id: 'E_W003', name: '樹皮帽', element: 'wood', tier: 'basic', slot: 'helmet', levelReq: 1, defBonus: 5, shopPrice: 130, npcSellPrice: 26 },
  { id: 'E_W004', name: '粗布衣', element: 'wood', tier: 'basic', slot: 'armor', levelReq: 1, defBonus: 8, hpBonus: 10, shopPrice: 150, npcSellPrice: 30 },
  { id: 'E_W005', name: '草鞋', element: 'wood', tier: 'basic', slot: 'boots', levelReq: 1, spdBonus: 4, shopPrice: 100, npcSellPrice: 20 },
  { id: 'E_W006', name: '綠葉護符', element: 'wood', tier: 'basic', slot: 'accessory', levelReq: 1, hpBonus: 15, shopPrice: 90, npcSellPrice: 18 },
];
```

---

## 任務三：P1 — 掉落計算邏輯

在 `server/tickEngine.ts` 的戰鬥結算函數中，加入掉落計算邏輯。

### 核心掉落公式（請直接複製使用）

```typescript
// 在 server/tickEngine.ts 中加入以下函數

interface DropContext {
  monsterId: string;
  monsterElement: WuxingElement;
  attackerElement: WuxingElement;
  attackerNatalStats: Record<WuxingElement, number>; // 五行比例 0~100
  luckyValue: number; // 幸運值 0~1000（對應金屬性高的玩家）
  hasTaskBonus: boolean;
}

function calculateFinalDropRate(
  baseRate: number,
  equipmentElement: WuxingElement,
  ctx: DropContext
): number {
  // 1. 五行剋制加成（攻擊屬性 vs 怪物屬性）
  const elementBonus = getWuxingDropBonus(ctx.attackerElement, ctx.monsterElement);
  
  // 2. 命格共鳴加成（玩家命格 vs 掉落裝備屬性）
  const natalRatio = ctx.attackerNatalStats[equipmentElement] ?? 20;
  const natalBonus = Math.max(0.5, Math.min(1.5, 1 + (natalRatio - 20) * 0.005));
  
  // 3. 幸運加成（金屬性越高，幸運值越高）
  const luckyBonus = 1 + ctx.luckyValue / 1000;
  
  // 4. 任務加成
  const taskBonus = ctx.hasTaskBonus ? 1.2 : 1.0;
  
  return baseRate * elementBonus * natalBonus * luckyBonus * taskBonus;
}

function getWuxingDropBonus(attacker: WuxingElement, defender: WuxingElement): number {
  const WUXING_OVERCOME: Record<WuxingElement, WuxingElement> = {
    wood: 'earth', fire: 'metal', earth: 'water', metal: 'wood', water: 'fire'
  };
  const WUXING_GENERATE: Record<WuxingElement, WuxingElement> = {
    wood: 'fire', fire: 'earth', earth: 'metal', metal: 'water', water: 'wood'
  };
  if (WUXING_OVERCOME[attacker] === defender) return 1.5;
  if (WUXING_GENERATE[attacker] === defender) return 0.8;
  return 1.0;
}
```

### 低保系統函數

```typescript
async function checkPityAndGetGuaranteedTier(
  agentId: number
): Promise<'basic' | 'mid' | 'high' | null> {
  const db = await getDb();
  const counter = await db.query.agentDropCounters.findFirst({
    where: eq(agentDropCounters.agentId, agentId)
  });
  
  if (!counter) return null;
  
  if (counter.noHighStreak >= 75) return 'high';
  if (counter.noMidStreak >= 30) return 'mid';
  if (counter.noDropStreak >= 15) return 'basic';
  
  return null;
}

async function updateDropCounters(
  agentId: number,
  droppedTier: 'basic' | 'mid' | 'high' | 'legendary' | null
): Promise<void> {
  const db = await getDb();
  
  if (!droppedTier) {
    // 未掉落，所有計數 +1
    await db.update(agentDropCounters)
      .set({
        noDropStreak: sql`no_drop_streak + 1`,
        noMidStreak: sql`no_mid_streak + 1`,
        noHighStreak: sql`no_high_streak + 1`,
        lastUpdated: Date.now(),
      })
      .where(eq(agentDropCounters.agentId, agentId));
  } else {
    // 有掉落，重置對應計數
    const updates: Partial<typeof agentDropCounters.$inferInsert> = {
      noDropStreak: 0,
      lastUpdated: Date.now(),
    };
    if (['mid', 'high', 'legendary'].includes(droppedTier)) updates.noMidStreak = 0;
    if (['high', 'legendary'].includes(droppedTier)) updates.noHighStreak = 0;
    
    await db.update(agentDropCounters)
      .set(updates)
      .where(eq(agentDropCounters.agentId, agentId));
  }
}
```

---

## 任務四：P1 — tRPC 端點

在 `server/routers/game.ts` 新增以下端點：

```typescript
// 取得玩家背包
getInventory: protectedProcedure.query(async ({ ctx }) => {
  const db = await getDb();
  const agent = await getOrCreateAgent(ctx.user.id, db);
  return db.query.agentInventory.findMany({
    where: eq(agentInventory.agentId, agent.id),
    orderBy: [desc(agentInventory.obtainedAt)],
  });
}),

// 裝備/卸下裝備
equipItem: protectedProcedure
  .input(z.object({
    inventoryId: z.number(),
    action: z.enum(['equip', 'unequip']),
  }))
  .mutation(async ({ ctx, input }) => {
    // 實作裝備邏輯，確認 slot 衝突
  }),

// 取得掉落計數（用於 UI 顯示低保進度）
getDropCounters: protectedProcedure.query(async ({ ctx }) => {
  const db = await getDb();
  const agent = await getOrCreateAgent(ctx.user.id, db);
  return db.query.agentDropCounters.findFirst({
    where: eq(agentDropCounters.agentId, agent.id),
  });
}),
```

---

## 任務五：P1 — 前端背包 UI

在 `client/src/pages/VirtualWorld.tsx` 的「旅人」Tab 中，新增裝備欄位顯示：

**裝備欄位（8 個 slot）：**
- 武器（weapon）
- 頭盔（helmet）
- 上衣（armor）
- 鞋子（boots）
- 飾品 1（accessory）
- 飾品 2（accessory，第二個）
- 寵物 1（pet，未來擴充）
- 寵物 2（pet，未來擴充）

**背包顯示：**
- 格狀排列（每行 5 格，共 10 行 = 50 格）
- 每格顯示裝備名稱、屬性顏色、稀有度邊框
- 點擊裝備顯示詳細資訊 Modal（屬性加成、詞條、來源）

**稀有度顏色對應（配合 GD-015B 第九章美術規格）：**
```typescript
const TIER_COLORS = {
  basic: 'text-gray-400 border-gray-600',
  mid: 'text-blue-400 border-blue-600',
  high: 'text-purple-400 border-purple-600',
  legendary: 'text-yellow-400 border-yellow-600 shadow-yellow-500/50',
};
```

---

## 驗證清單

完成後請確認以下項目：

- [ ] `pnpm db:push` 成功，五張新表建立完成
- [ ] 木屬性初階裝備種子資料插入成功（6 件裝備、10 隻怪物的掉落表）
- [ ] Tick 引擎戰鬥結算時，掉落計算函數正確呼叫
- [ ] 低保計數器在每次戰鬥後正確更新
- [ ] 背包 UI 可以顯示已獲得的裝備
- [ ] 裝備/卸下功能正常運作

---

## 完成後請回覆

完成後請在倉庫 `MANUS-AGENTS/FOR-SYSTEM/REPLIES/` 目錄新增回覆文件，格式如下：

```
MANUS-AGENTS/FOR-SYSTEM/REPLIES/20260324-GD021-完成回報.md
```

內容包含：完成的任務項目、遇到的問題與解決方式、尚未完成的項目與原因。

---

*虛相世界沙盒 Agent — 2026-03-24*
