# 功能提案模板

> **使用說明**：複製這個檔案，填寫所有必填欄位，然後以 `PROPOSAL-[日期]-[功能名稱]-PROPOSAL.md` 命名儲存。
> 所有標有 `[必填]` 的欄位都必須填寫，否則提案會被退回。

---

## 提案基本資訊

| 欄位 | 內容 |
|------|------|
| **提案編號** | PROPOSAL-[YYYYMMDD]-[功能名稱] |
| **提案日期** | YYYY/MM/DD |
| **提案者** | 天命輔助系統 |
| **狀態** | `draft` / `pending` |
| **優先級** | 🔴 高 / 🟡 中 / 🟢 低 |

---

## 功能說明 [必填]

### 功能名稱
[填寫功能名稱]

### 功能描述
[用 2-3 段文字說明這個功能是什麼、解決什麼問題、用戶體驗是什麼]

### 用戶使用情境
[描述用戶如何使用這個功能，從進入頁面到完成操作的完整流程]

---

## 技術規格 [必填]

### 影響的現有檔案
> 必須精確到檔案路徑，說明修改什麼

| 檔案路徑 | 修改內容說明 |
|----------|-------------|
| `client/src/pages/XXX.tsx` | [說明要加什麼] |
| `server/routers.ts` | [說明要加什麼 procedure] |

### 新增的檔案
> 如果不需要新增檔案，填「無」

| 檔案路徑 | 用途說明 |
|----------|---------|
| `client/src/pages/NewPage.tsx` | [說明用途] |
| `client/src/components/NewComponent.tsx` | [說明用途] |

### 資料庫異動 [必填]
> 如果不需要異動資料庫，填「無」

```typescript
// 如果需要新增資料表，提供 Drizzle schema 草稿
// 例：
export const newTable = mysqlTable('new_table', {
  id: int('id').primaryKey().autoincrement(),
  userId: varchar('user_id', { length: 255 }).notNull(),
  // ...
});
```

### 新的 tRPC Procedures
> 如果不需要新增 API，填「無」

```typescript
// 說明需要哪些新的 tRPC procedures
// 例：
newFeature: {
  getData: publicProcedure
    .input(z.object({ id: z.string() }))
    .query(async ({ input }) => {
      // 說明這個 procedure 要做什麼
    }),
}
```

---

## UI 設計說明 [必填]

### 頁面位置
[說明這個功能在哪個頁面，或需要新建哪個路由]

### 版面配置
[用文字或 ASCII 描述版面，不需要寫程式碼]

```
例：
┌─────────────────────────────┐
│  標題區塊（gold-gradient）   │
├─────────────────────────────┤
│  功能內容（glass-card）      │
│  [按鈕] [按鈕]               │
└─────────────────────────────┘
```

### 使用的 CSS 類別
[列出會用到的現有 CSS 類別，不能自創新類別]

- `.glass-card` — [用在哪裡]
- `.text-gold-gradient` — [用在哪裡]
- [其他]

---

## 衝突檢查 [必填]

### 與現有功能的關係
[說明這個功能是否與現有功能有重疊，如何共存]

### 影響的現有功能
[列出這個功能可能影響到的現有功能，例如：修改導覽列會影響所有頁面]

---

## 審核結果（由天命主系統填寫）

**狀態**：[待填寫]
**審核時間**：[待填寫]
**審核意見**：[待填寫]
